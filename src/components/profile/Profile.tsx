import { Container, Grid } from "@mui/material";
import React, { FC } from "react";

const Profile: FC = () => {
  return (
    <Container fixed>
      <Grid container></Grid>
    </Container>
  );
};

export default Profile;
